apiVersion: apps/v1
kind: Deployment
metadata:
  name: products-service
  namespace: flask-microservices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: products-service
  template:
    metadata:
      labels:
        app: products-service
    spec:
      containers:
        - name: products-service
          image: products-service:latest
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 5000
          env:
            - name: SERVICE_NAME
              value: products-service
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 3
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: flask-microservices
spec:
  type: ClusterIP
  selector:
    app: products-service
  ports:
    - name: http
      port: 5000
      targetPort: http
